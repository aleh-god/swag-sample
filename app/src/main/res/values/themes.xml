<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SwaggerApplication" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/primarySwagger</item>
        <item name="colorPrimaryVariant">@color/primaryVariantSwagger</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="backgroundColor">@color/backgroundSwagger</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/primarySwagger</item>
        <item name="colorSecondaryVariant">@color/primaryVariantSwagger</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
</resources>